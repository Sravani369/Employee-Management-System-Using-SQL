# Employee-Management-System-Using-SQL
Here is the SQL  the SQL code for creating a simple employee management system, encompassing the database and table schema, along with fundamental CRUD (Create, Read, Update, Delete) operations using SQL queries. 
